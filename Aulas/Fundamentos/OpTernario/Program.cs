Console.WriteLine("Operador ternário"); // Retornam apenas true ou false

String resp = 30 > 20 ? "Sim" : "Não";

Console.WriteLine(resp);

bool ativo = true;

String status = ativo ? "Ativo" : "Inativo";

Console.WriteLine(status);